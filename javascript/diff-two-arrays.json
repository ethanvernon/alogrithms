{"index.js":"function diffArray(arr1, arr2) {\n\n  /* Compare two arrays and return a new array with any \n  items only found in one of the two given \n  arrays, but not both. In other words, return the \n  symmetric difference of the two arrays.*/\n  \n  //holding array for unique values\n  var newArr = [];\n\n  //loop through first array, add unique values from it\n  for (let i=0;i<arr1.length;i++) {\n    if (!arr2.includes(arr1[i])) {\n      newArr.push(arr1[i]);\n    }\n  }\n\n  //loop through second array, add unique values from it\n  for (let i=0;i<arr2.length;i++) {\n    if (!arr1.includes(arr2[i])) {\n      newArr.push(arr2[i]);\n    }\n  }\n  \n  return newArr;\n}\n\ndiffArray([1, 2, 3, 5], [1, 2, 3, 4, 5]);"}